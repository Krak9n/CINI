Basically everything is already explained.
Here you should put the breakpoint manually at the **puts** call. Remember to disassemble beforehand.

Then just print in hexadecimal form $rbp register with offset specified. 
